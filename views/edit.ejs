<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>

<body>
    <h1>ToDoアプリ</h1>
    <h2>編集</h2>
    <form action="/edit/<%= todo._id %>?_method=PUT" method="POST">
        <div>
            <label for="title">やること：</label>
            <!-- idはlabelのforと連動。nameはform送信時にURLにつくやつ？ -->
            <input type="text" id="title" name="title" required value=<%= todo.title %>>
        </div>
        <div>
            <label for="limit_day">何日まで：</label>
            <input type="date" id="limit_day" name="limit_day" required value=<%= todo.limit_day %>>
            <label for="limit_time">何時まで（任意）：</label>
            <input type="time" id="limit_time" name="limit_time" value=<%= todo.limit_time %>>
        </div>
        <div>
            <label for="importance">重要度</label>
            <select name="importance" id="importance" value=<%= todo.inportance %>>
                <option value="低">低</option>
                <option value="中" selected>中</option>
                <option value="高">高</option>
                <option value="緊急">緊急</option>
            </select>
        </div>
        <div>
            <label for="check">進捗</label>
            <input type="radio" id="done" name="check" value="true" <%= todo.check ? "checked" : "" %>>
            <label for="done">完了</label>
            <input type="radio" id="notyet" name="check" value="false" <%= todo.check ? "" : "checked" %>>
            <label for="done">未完了</label>
        </div>
        <a href="/">戻る</a>
        <button>変更する</button>
    </form>
     <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
</body>

</html>