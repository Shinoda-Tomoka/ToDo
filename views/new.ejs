<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>

<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">ToDoアプリ</h1>
            <div class="text-center card-header bg-success text-white mb-4 rounded-2">
                新規登録
            </div>
            <form action="/" method="POST" class="card bg-success bg-opacity-10 border-success p-4 shadow">
                <div class="mb-3">
                    <label for="new_title" class="form-label">やること：</label>
                    <!-- idはlabelと連動。nameはform送信時にURLにつくやつ？ -->
                    <input type="text" id="new_title" name="title" required>
                </div>
                <div class="mb-3">
                    <label for="new_limit_day" class="form-label">何日まで：</label>
                    <input type="date" id="new_limit_day" name="limit_day" required value="<%= new Date().toISOString().slice(0,10) %>">
                </div>
                <div class="mb-3">
                    <label for="new_limit_time">何時まで（任意）：</label>
                    <input type="time" id="new_limit_time" name="limit_time" value="23:59">
                </div>
                <div class="mb-3">
                    <label for="new_importance">重要度</label>
                    <select name="importance" id="new_importance">
                        <option value="低">低</option>
                        <option value="中" selected>中</option>
                        <option value="高">高</option>
                        <option value="緊急">緊急</option>
                    </select>
                </div>
                <div class="d-flex justify-content-between">
                    <a href="/" class="btn btn-outline-secondary">戻る</a>
                    <button type="submit" class="btn btn-success btn-sm">追加する</button>
                </div>
            </form>
    </div>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz"
            crossorigin="anonymous"></script>
</body>

</html>