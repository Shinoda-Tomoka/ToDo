<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

</head>

<body>
    <h1>ToDoアプリ</h1>
    <div>
        <h2>やることリスト</h2>
        <% //未完了のもののみ表示 %>
            <ul>
                <% todos.forEach(todo=> { %>
                    <% if (!todo.check) { %>
                        <li>
                            <%= todo.title %> (<%= todo.limit_day %>
                            <%= todo.limit_time %>)
                            重要度: <%= todo.importance %>
                            <%= todo.check ? "完了" : "未完了" %>
                            <form action="/check/<%= todo._id %>" method="POST" style="display:inline;">
                                <input type="checkbox" name="check" onchange="this.form.submit()">
                            </form>
                            <form action="/delete/<%= todo._id %>?_method=DELETE" method="POST" style="display:inline;">
                               <button type="submit">削除</button> 
                            </form>
                        </li>
                    <% } %>
                <% }) %>
            </ul>
    </div>

    <div>
        <h2>完了リスト</h2>
        <% //完了のもののみ表示 %>
            <ul>
                <% todos.forEach(todo=> { %>
                    <% if (todo.check) { %>
                        <li>
                            <%= todo.title %> (<%= todo.limit_day %>
                            <%= todo.limit_time %>)
                            重要度: <%= todo.importance %>
                            <%= todo.check ? "完了" : "未完了" %>
                        </li>
                    <% } %>
                <% }) %>
            </ul>
    </div>

    <a href="/new">新規登録</a>

</body>

</html>